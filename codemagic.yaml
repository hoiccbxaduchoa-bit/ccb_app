workflows:
  ios-build:
    name: Build iOS

    environment:
      flutter: stable

    scripts:
      - flutter clean
      - flutter pub get
      - flutter build ios --release

    artifacts:
      - build/ios/ipa/*.ipa
